# CONTINUATION ENGINE

The Continuation Engine saves file_path, current_hash, last_completed_symbol, open_blocks, imports declared, public APIs emitted, remaining plan, critic warnings, tests required, and last_chunk_id.
