# QUALITY GATES

Quality gates include format checks, lint, typecheck, unit tests, integration tests, contract tests, build, circular dependency scan, dead code scan, secret scan, architecture boundary check, API compatibility, regression, continuation integrity, and change migration checks.
