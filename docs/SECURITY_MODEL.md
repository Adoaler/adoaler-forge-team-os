# SECURITY MODEL

Security forbids dangerous deletes without approval, arbitrary shell execution, workspace escape, secret persistence, token logging, validation removal, remote script execution, and hardcoded credentials.
