# ARCHITECTURE

Architecture centers on a Codex plugin manifest, role skills, a local stdio MCP server, SQLite LTM, deterministic engines, and guard scripts. The Codex model remains the execution engine; this plugin owns planning state, memory, validation, review, repair, and release policy.
