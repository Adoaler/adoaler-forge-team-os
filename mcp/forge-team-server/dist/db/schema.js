export const schemaSql = [
    "CREATE TABLE IF NOT EXISTS schema_migrations (id TEXT PRIMARY KEY, applied_at TEXT NOT NULL)",
    "CREATE TABLE IF NOT EXISTS projects (id TEXT PRIMARY KEY, name TEXT NOT NULL, root_path TEXT NOT NULL, status TEXT NOT NULL DEFAULT 'active', brain_hash TEXT, created_at TEXT NOT NULL, updated_at TEXT NOT NULL)",
    "CREATE TABLE IF NOT EXISTS modules (id TEXT PRIMARY KEY, project_id TEXT NOT NULL, name TEXT NOT NULL, responsibilities TEXT NOT NULL DEFAULT '[]', owner TEXT, status TEXT NOT NULL DEFAULT 'planned', risk_level TEXT NOT NULL DEFAULT 'medium', created_at TEXT NOT NULL, updated_at TEXT NOT NULL)",
    "CREATE TABLE IF NOT EXISTS files (file_path TEXT PRIMARY KEY, project_id TEXT, module_id TEXT, status TEXT NOT NULL DEFAULT 'planned', current_hash TEXT, last_completed_symbol TEXT, open_blocks TEXT NOT NULL DEFAULT '[]', imports_declared TEXT NOT NULL DEFAULT '[]', public_api_emitted TEXT NOT NULL DEFAULT '[]', remaining_plan TEXT, critic_warnings TEXT NOT NULL DEFAULT '[]', tests_required TEXT NOT NULL DEFAULT '[]', last_chunk_id TEXT, coverage REAL DEFAULT 0, updated_at TEXT NOT NULL)",
    "CREATE TABLE IF NOT EXISTS file_chunks (id TEXT PRIMARY KEY, file_path TEXT NOT NULL, chunk_hash TEXT NOT NULL, summary TEXT NOT NULL, start_symbol TEXT, end_symbol TEXT, created_at TEXT NOT NULL)",
    "CREATE TABLE IF NOT EXISTS continuations (id TEXT PRIMARY KEY, file_path TEXT NOT NULL, current_hash TEXT NOT NULL, last_completed_symbol TEXT, open_blocks TEXT NOT NULL DEFAULT '[]', imports_declared TEXT NOT NULL DEFAULT '[]', public_api_emitted TEXT NOT NULL DEFAULT '[]', remaining_plan TEXT NOT NULL, critic_warnings TEXT NOT NULL DEFAULT '[]', tests_required TEXT NOT NULL DEFAULT '[]', status TEXT NOT NULL DEFAULT 'partial', created_at TEXT NOT NULL, updated_at TEXT NOT NULL)",
    "CREATE TABLE IF NOT EXISTS symbols (id TEXT PRIMARY KEY, project_id TEXT, file_path TEXT NOT NULL, name TEXT NOT NULL, kind TEXT NOT NULL, exported INTEGER NOT NULL DEFAULT 0, signature TEXT, hash TEXT, updated_at TEXT NOT NULL)",
    "CREATE TABLE IF NOT EXISTS tasks (id TEXT PRIMARY KEY, project_id TEXT, title TEXT NOT NULL, type TEXT NOT NULL, depends_on TEXT NOT NULL DEFAULT '[]', blocked_by TEXT NOT NULL DEFAULT '[]', allowed_files TEXT NOT NULL DEFAULT '[]', forbidden_files TEXT NOT NULL DEFAULT '[]', acceptance_criteria TEXT NOT NULL DEFAULT '[]', quality_gates TEXT NOT NULL DEFAULT '[]', required_agents TEXT NOT NULL DEFAULT '[]', risk_level TEXT NOT NULL DEFAULT 'medium', rollback_plan TEXT, memory_context TEXT NOT NULL DEFAULT '[]', expected_outputs TEXT NOT NULL DEFAULT '[]', status TEXT NOT NULL DEFAULT 'planned', created_at TEXT NOT NULL, updated_at TEXT NOT NULL)",
    "CREATE TABLE IF NOT EXISTS decisions (id TEXT PRIMARY KEY, project_id TEXT, topic TEXT NOT NULL, decision TEXT NOT NULL, reason TEXT NOT NULL, linked_task_id TEXT, created_at TEXT NOT NULL)",
    "CREATE TABLE IF NOT EXISTS bugs (id TEXT PRIMARY KEY, project_id TEXT, title TEXT NOT NULL, category TEXT NOT NULL, symptoms TEXT NOT NULL, affected_files TEXT NOT NULL DEFAULT '[]', root_cause TEXT, fix_summary TEXT, status TEXT NOT NULL DEFAULT 'open', created_at TEXT NOT NULL, updated_at TEXT NOT NULL)",
    "CREATE TABLE IF NOT EXISTS critic_reports (id TEXT PRIMARY KEY, project_id TEXT, scope TEXT NOT NULL, target TEXT NOT NULL, score INTEGER NOT NULL, verdict TEXT NOT NULL, blocking_issues TEXT NOT NULL DEFAULT '[]', non_blocking_issues TEXT NOT NULL DEFAULT '[]', required_fix_tasks TEXT NOT NULL DEFAULT '[]', memory_updates TEXT NOT NULL DEFAULT '[]', risk_level TEXT NOT NULL, created_at TEXT NOT NULL)",
    "CREATE TABLE IF NOT EXISTS quality_runs (id TEXT PRIMARY KEY, project_id TEXT, task_id TEXT, status TEXT NOT NULL, failed_gates TEXT NOT NULL DEFAULT '[]', logs_summary TEXT NOT NULL, suspected_causes TEXT NOT NULL DEFAULT '[]', recommended_repair_tasks TEXT NOT NULL DEFAULT '[]', created_at TEXT NOT NULL)",
    "CREATE TABLE IF NOT EXISTS checkpoints (id TEXT PRIMARY KEY, project_id TEXT, label TEXT NOT NULL, file_hashes TEXT NOT NULL DEFAULT '{}', task_snapshot TEXT NOT NULL DEFAULT '[]', created_at TEXT NOT NULL)",
    "CREATE TABLE IF NOT EXISTS architecture_rules (id TEXT PRIMARY KEY, project_id TEXT, rule TEXT NOT NULL, applies_to TEXT NOT NULL DEFAULT '[]', severity TEXT NOT NULL DEFAULT 'high', created_at TEXT NOT NULL)",
    "CREATE TABLE IF NOT EXISTS dependencies (id TEXT PRIMARY KEY, project_id TEXT, from_module TEXT, to_module TEXT, from_file TEXT, to_file TEXT, allowed INTEGER NOT NULL DEFAULT 1, reason TEXT, updated_at TEXT NOT NULL)",
    "CREATE TABLE IF NOT EXISTS api_contracts (id TEXT PRIMARY KEY, project_id TEXT, module_id TEXT, name TEXT NOT NULL, contract_json TEXT NOT NULL DEFAULT '{}', version TEXT NOT NULL DEFAULT '1.0.0', status TEXT NOT NULL DEFAULT 'active', updated_at TEXT NOT NULL)",
    "CREATE TABLE IF NOT EXISTS test_inventory (id TEXT PRIMARY KEY, project_id TEXT, module_id TEXT, test_path TEXT NOT NULL, target_path TEXT, kind TEXT NOT NULL, status TEXT NOT NULL DEFAULT 'planned', updated_at TEXT NOT NULL)",
    "CREATE TABLE IF NOT EXISTS risk_register (id TEXT PRIMARY KEY, project_id TEXT, risk TEXT NOT NULL, impact TEXT NOT NULL, mitigation TEXT NOT NULL, status TEXT NOT NULL DEFAULT 'open', created_at TEXT NOT NULL, updated_at TEXT NOT NULL)",
    "CREATE TABLE IF NOT EXISTS project_state (key TEXT PRIMARY KEY, value TEXT NOT NULL, updated_at TEXT NOT NULL)",
    "CREATE TABLE IF NOT EXISTS change_requests (id TEXT PRIMARY KEY, project_id TEXT, user_request TEXT NOT NULL, old_requirement TEXT, new_requirement TEXT NOT NULL, reason TEXT, affected_modules TEXT NOT NULL DEFAULT '[]', affected_files TEXT NOT NULL DEFAULT '[]', affected_tasks TEXT NOT NULL DEFAULT '[]', affected_tests TEXT NOT NULL DEFAULT '[]', affected_contracts TEXT NOT NULL DEFAULT '[]', migration_plan TEXT, rollback_plan TEXT, risk_level TEXT NOT NULL DEFAULT 'medium', status TEXT NOT NULL DEFAULT 'proposed', created_at TEXT NOT NULL, updated_at TEXT NOT NULL)",
    "CREATE TABLE IF NOT EXISTS migrations (id TEXT PRIMARY KEY, project_id TEXT, change_request_id TEXT, title TEXT NOT NULL, steps TEXT NOT NULL DEFAULT '[]', rollback_plan TEXT, status TEXT NOT NULL DEFAULT 'planned', created_at TEXT NOT NULL, updated_at TEXT NOT NULL)",
    "CREATE TABLE IF NOT EXISTS releases (id TEXT PRIMARY KEY, project_id TEXT, checkpoint_id TEXT, version TEXT NOT NULL, notes TEXT NOT NULL, status TEXT NOT NULL DEFAULT 'planned', created_at TEXT NOT NULL, updated_at TEXT NOT NULL)",
    "CREATE INDEX IF NOT EXISTS idx_tasks_status ON tasks(status)",
    "CREATE INDEX IF NOT EXISTS idx_symbols_name ON symbols(name)",
    "CREATE INDEX IF NOT EXISTS idx_files_status ON files(status)",
    "CREATE INDEX IF NOT EXISTS idx_decisions_topic ON decisions(topic)",
    "CREATE INDEX IF NOT EXISTS idx_bugs_category ON bugs(category)"
];
