interface:
  display_name: "Security Agent"
  short_description: "Review secrets, filesystem, network, input, auth, and command execution risks."
  default_prompt: "Use Security Agent inside Adoaler Forge Team OS."
